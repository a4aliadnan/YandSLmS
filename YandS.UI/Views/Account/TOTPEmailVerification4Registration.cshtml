@model YandS.UI.Models.TOTP4EmailViewModelGet
@{
    ViewBag.Title = "Verify Email";
}

<h2>@ViewBag.Title.</h2>

@using (Html.BeginForm("TOTPEmailVerification4Registration", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.Hidden("UserId", @Model.UserId)
    @Html.Hidden("Provider", @Model.Provider)

    <p>You must activate your account using a provided e-mail.  Please enter a valid email to generate an account activation link.</p>
    <br />
    <h4>Enter security PIN code</h4>
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Verify My Identity" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}





